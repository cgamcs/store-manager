# Instalación

Para instalar las dependencias hay que ejecutar el comando `npm install`.

# Regenerar Prisma

Para regenerar los modelos y configuraciones de prisma se debe ejecutar el comando `npx prisma generate`.